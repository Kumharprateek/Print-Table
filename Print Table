#include <stdio.h>
https://www.onlinegdb.com/#editor_1int main()
{
    
    {
        int i,j;
        printf("Enter a number:: ");
        scanf("%d",&j);
        for(i=1;i<=100;++i)
        {
            printf("%d * %d = %d \n",i,j,i * j );
        }
    }


    return 0;
}
